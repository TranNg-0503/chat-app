//! Every config related to DaisyUI theme should be here

export const THEMES = {
  Emerald: "emerald",
  Night: "night",
}
// By default "emerald" will be selected (check index.html file)
export const THEME_KEYS = Object.values(THEMES);